<!DOCTYPE html>
html(lang="es")
    head
        include partials/header
        //- link(rel="stylesheet", href="/awesome-notifications/dist/style.css")
        //- script(src='/awesome-notifications/dist/index.var.js')
        //- script(src='/js/funciones/notificaciones.js')
    body
        header.barra
            h1  UpTask - Administrador de Proyectos
            a(href="/cerrar-sesion") Cerrar Sesión
            

        main.contenedor
            aside.contenedor-proyectos
                .panel.crear-proyecto
                    a.boton.nuevo-proyecto(href="/nuevo-proyecto") Nuevo Proyecto
                        span.fas.fa-plus.fa-lg

                .panel.lista-proyectos
                    h2 Proyectos
                    ul#proyectos.proyectos
                        if (typeof proyectos !== 'undefined' && proyectos.length > 0)
                            each proyecto in proyectos
                                li  
                                    a(href=`/proyectos/${proyecto.url}`) #{proyecto.nombre}
                
            block contenido

            //- pre #{vardump(proyectos)}
    
        //- if  locals.usuario
        //-             script. 
        //-                 //-console.log(!{JSON.stringify(locals.usuario)})
        //-                 bienvenido(!{JSON.stringify(locals.usuario)})

    script(src="/dist/bundle.js")
            