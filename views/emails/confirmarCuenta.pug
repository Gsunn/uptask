doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
    body 
        h2 Activar Cuenta 
        p Ya estas casi inscrito, presiona el siguiente enlace
        a(href=`${confirmarUrl}`) Activar Cuenta
        p Si no puedes acceder, visita #{confirmarUrl}
        p Si no solicitaste este email, ignoralo 