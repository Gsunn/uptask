<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
    body 
        h2 Restablecer Password 
        p Has solicitado restablecer password, haz click en el enlace 
        a(href=`${resetUrl}`) Resetear Password
        p Si no puedes acceder, visita #{resetUrl}
        p Si no solicitaste este email, ignoralo 
